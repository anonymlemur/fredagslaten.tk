# Dependencies
node_modules/

# Unused / non-app static content
spotify-playlist-voting/public/adrian_meme/
spotify-playlist-voting/public/friskel/
spotify-playlist-voting/public/html1/

# Secrets / credentials (never commit)
.env
spotify-playlist-voting/spotifyApiDetails.json
spotify-playlist-voting/emailDetails.json
spotify-playlist-voting/firebaseServiceAccountKey.json

# Runtime data — flat-file "DB" + per-week backups.
# Not needed in VCS; these get migrated into SQLite (see PLAN.md).
spotify-playlist-voting/appData.json
spotify-playlist-voting/appData.jsonOld
spotify-playlist-voting/appData copy.json
spotify-playlist-voting/backup/
spotify-playlist-voting/playlists.json
appData copy.json

# Generated SQLite database
spotify-playlist-voting/data/*.db
*.sqlite
*.sqlite3

# Logs
*.log

# Editor / dev leftovers
*.save
*_old
*.jsonOld

# Vim swap files
*.sw[a-p]
[._]*.sw[a-p]

# MCP local memory store (machine-local, not shared)
.mcp/
